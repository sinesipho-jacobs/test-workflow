name: Call Reusable Robot Test Workflow

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  call-tests:
    uses: sinesipho-jacobs/test-workflow/blob/main/.github/workflows/reusable-workflow.yaml@main

