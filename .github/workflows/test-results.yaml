name: "Run Robot Framework Tests and Parse Results"

on:
  push:
    branches:
      - main

jobs:
  run-tests-and-parse-results:
    uses: ./.github/workflows/reusable-workflow.yaml  # Calling the reusable workflow
    with:
      test-results-path: "webapp_tests/output.xml"
