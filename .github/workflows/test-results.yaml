name: Test Results

on:
  workflow_run:
    workflows: ["Run Tests"]
    types:
      - completed

jobs:
  parse-results:
    uses: sinesipho-jacobs/test-workflow/blob/main/.github/workflows/reusable-workflow.yaml@main  # Ensure correct repo & path
    with:
      some-input: some-value  # If needed, pass inputs
      
    secrets: inherit  # If needed, inherit secrets
